# ngrok Configuration for Q-Collector Mobile Testing
# Version: v0.7.8-dev
# Purpose: Expose both frontend (3000) and backend (5000) for mobile testing

version: 3
agent:
  authtoken: 2lhSOkILcTHDiXoxLIzWWRMm9aC_29bry5ejwQEVNhwyUjmFr

tunnels:
  frontend:
    addr: 3000
    proto: http
    # hostname: your-custom-domain.ngrok.io  # Uncomment if you have a custom domain
    inspect: true
    bind_tls: true

  backend:
    addr: 5000
    proto: http
    # hostname: your-custom-backend.ngrok.io  # Uncomment if you have a custom domain
    inspect: true
    bind_tls: true
